@page "/authentication/{action}"
@inherits BasePage
@inject IOptions<IdentityProvider> IdentityProvider

@if (IdentityProvider.Value.Enable)
{
    <CustomRemoteAuthenticatorView Action="@Action">
        <LogOutSucceeded>
            <RedirectTo Target="" />
        </LogOutSucceeded>
    </CustomRemoteAuthenticatorView>
}
else
{
    <Result Status="403"
            Title=@("Forbidden")
            SubTitle=@("Sorry, you are not authorized to access this page.") />
}

@code{
    [Parameter]
    public string Action { get; set; }
}
