@if (Value != null && Value.Count != 0)
{
    <span class="itemdisplay">
        <span class="mdi @(Value.Count == 1 ? "mdi-tag" : "mdi-tag-multiple")">@string.Join(" ; ", Value)</span>
    </span>
}

@code {
    [Parameter]
    public ICollection<string> Value { get; set; }
}
